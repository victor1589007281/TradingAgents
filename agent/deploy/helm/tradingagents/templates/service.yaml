apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradingagents.fullname" . }}
  labels:
    {{- include "tradingagents.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tradingagents.selectorLabels" . | nindent 4 }}
